<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera captures staged for <input type="file" capture>. -->
    <cache-path name="uploads" path="uploads/" />
    <!-- Completed downloads, shared read-only via a grant-scoped content:// URI. -->
    <external-path name="downloads" path="Download/" />
    <external-files-path name="app_downloads" path="Download/" />
</paths>
